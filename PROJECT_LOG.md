# 프로젝트 진행 기록

이 문서는 이 프로젝트를 함께 진행하며 쌓인 맥락을 정리한 기록이다. `rules.md`/`changelog.md`가 "예측 규칙의 변천사"를 남긴다면, 이 문서는 "시스템 자체의 구축 과정"을 남긴다.

## 프로젝트 개요

투자 자문이 아닌 실험 프로젝트. 규칙 기반(rule-based) 시스템으로 매일 국내/미국/코인 방향(상승/하락/보합) 의견을 내고, 결과와 비교하며 규칙을 점진적으로 개선한다. 세부 구조는 `README.md`, 원칙은 `CLAUDE.md` 참고.

## 지금까지 있었던 일

### 1. 로컬 환경에서 실행 가능 여부 확인
이전 세션에서는 "가상화를 사용할 수 없습니다" 에러로 스크립트를 한 번도 실제 실행해보지 못한 상태였다. 이번 세션(로컬 PowerShell 직접 실행 환경)에서 `quant_stats.py`(국내: FinanceDataReader, 미국: yfinance, 코인: CoinGecko API), `portfolio_log.py`(open/close/summary), `backtest_script.py`를 전부 처음부터 끝까지 실행해 검증했다.

발견하고 고친 문제:
- `pip install FinanceDataReader`는 실패한다 — 실제 PyPI 패키지명은 `finance-datareader`(소문자). `backtest_script.py` 안내 문구 수정.
- Windows 콘솔 기본 인코딩(cp949)에서 em dash(—) 등 특수문자 출력 시 `UnicodeEncodeError` 발생. `quant_stats.py`, `portfolio_log.py`, `backtest_script.py`에 `sys.stdout.reconfigure(encoding="utf-8")` 추가해 환경변수 설정 없이도 항상 안전하게 출력되도록 수정.

### 2. 10년치 백테스트 실행 결과 (2016-07-27 ~ 2026-07-25)
`backtest/report_v1.md`, `backtest/raw_results.csv`에 저장됨. rules.md v1 중 가격 기반 신호(추세·변동성·해외연동)만 반영한 "부분집합" 검증이며, 뉴스 톤 신호는 과거 데이터가 없어 제외됨.

| 시장 | 콜 수 | 적중률 | 규칙 누적수익 | Buy&Hold | 규칙이 나았나 |
|---|---|---|---|---|---|
| 국내(코스피) | 2,428건 | 71.5% | +392.04% | +230.39% | 예 |
| 미국(S&P500) | 2,490건 | 74.7% | -18.5% | +242.11% | 아니오 |
| 코인(BTC) | 3,628건 | 47.1% | +203.49% | +9,695.72% | 아니오 |

**중요한 함정**: "보합/혼조" 콜은 실제 변동이 ±1% 이내면 무조건 "적중"으로 처리되는데, 지수는 원래 대부분의 날이 이 범위 안에서 움직인다. 그래서 미국 시장은 적중률 74.7%인데도 실제 수익은 -18.5%(마이너스)다 — "적중률"과 "돈이 됐는지"가 따로 논다. 즉 가격 신호만으로는 국내 시장 정도만 근거가 있어 보이고, 미국·코인은 방향을 맞히는 게 랜덤과 큰 차이 없거나 오히려 해가 된다. 이 결과는 아직 `changelog.md`에 정식 반영되지 않았음 — 다음 규칙 조정 시 근거로 검토할 것.

### 3. 자동화 인프라 구축
- 세션 종속 예약(Claude Code 자체 cron)은 `durable: true`를 줘도 이 환경에서는 세션이 꺼지면 사라짐 — 확인 후 폐기.
- 진짜 무인 자동화(로컬 컴퓨터가 꺼져 있어도 매일 실행)를 위해 **클라우드 예약 에이전트(routine)** 방식으로 전환.
- 클라우드 에이전트는 로컬 파일에 접근 불가 → 이 프로젝트를 git 저장소화하고 GitHub 비공개 저장소로 push: **https://github.com/Yujin-park1111/claude_project**
- 로컬 git/GitHub CLI(`gh`) 설치 및 `gh auth login`, claude.ai GitHub 연동 완료.
- claude.ai 클라우드 예약 에이전트(routine) 2개 등록 완료:
  - **아침 브리핑** (`trig_01QXb8haMwgLrHGCZLJoK2Rq`) — 매일 07:03 KST
  - **마감 비교 브리핑** (`trig_016kEyR8EGUwxzg6i9s3DrEP`) — 평일 17:27 KST
- 페르소나 도입: **클대리**(작성자, 과장 진급이 목표) + **부사장**(같은 세션 안에서 페르소나 전환, 비판적 검토) 2라운드 검토 루프. 기준은 `report_policy.md` 참고. 클라우드 세션엔 별도 subagent 도구가 없어서, "부사장 역할"은 같은 에이전트가 명시적으로 페르소나를 전환해 자기 초안을 비판하는 방식으로 구현.

### 4. 알림 채널 시도 (2026-07-25 밤)
- Gmail MCP 커넥터는 **초안 생성(create_draft)만 지원, 실제 발송 도구 없음** — 처음엔 "실제 발송해라"라고 시켰지만 애초에 안 되는 기능이었음. 지금은 초안 생성만 하도록 프롬프트 수정함.
- Make.com(웹훅 → Gmail 발송)으로 실제 자동 발송 우회 시도 — 시나리오 자체는 실행 기록상 "Success"로 뜨는데 실제 수신함/스팸함에서 확인 안 됨 (원인 미해결, 다음에 볼 것).
- 텔레그램 봇으로 전환 예정 — 유진님이 내일 봇 토큰 전달 예정. 카카오톡(200자 제한)과 달리 텔레그램은 메시지당 4096자까지 가능.

### 5. 데이터 정확도 개선 (2026-07-25 밤, 자율 진행)
- **문제 발견**: 클라우드 샌드박스에서 `quant_stats.py`가 쓰는 외부 API(네이버 fchart, Yahoo Finance, CoinGecko)가 전부 네트워크 403으로 차단됨 (로컬은 정상). claude.ai 환경 설정에서 네트워크 허용 도메인을 열 수 있는 UI가 있는지 찾아봤으나 발견 못함 (Team/Enterprise 플랜 전용일 가능성 — GitHub 앱 설치 때 만났던 것과 같은 벽으로 추정).
- **해결**: Alpha Vantage 금융 데이터 MCP 커넥터(`https://mcp.alphavantage.co/mcp`, 계정에 "주식api"로 연결됨, 무료 API 키)를 두 예약에 연결. quant_stats.py 실패 시 이 커넥터로 대체하도록 프롬프트 수정 — Gmail처럼 Anthropic 인프라를 거쳐 우회되므로 네트워크 차단을 안 받음. 추가로 RSI/MACD/뉴스 감성 점수까지 이 커넥터로 가져올 수 있어짐.
- **백테스트로 신호 검증** (`backtest/report_v1.md`, 10년치): 기존 신호(추세/변동성/해외연동)에 RSI(14) 반전 신호, MACD 골든/데드크로스를 각각 추가해 시장별 성과 비교.
  - 국내: 추가 신호 없이 기존(v1)이 최선 (Sharpe 2.57)
  - 미국: RSI 추가가 확실히 개선 (누적수익 -18.5% → +23.69%, Sharpe -0.19 → +0.22) → 예약 프롬프트에 반영 완료
  - 코인: MACD가 근소하게 나음 (개선폭 작아 확신도 낮음, 보류)
- **적중률 지표 버그 수정**: "보합/혼조 콜은 ±1% 이내면 무조건 적중" 규칙 때문에 전체 적중률이 실제 수익성과 따로 노는 문제 발견 (2026-07-26 아침 리포트에서 미국 적중률 74.7%인데 수익 -18.5%로 처음 포착). `backtest_script.py`에 "방향성 콜(상승/하락)만 집계한 적중률" 지표를 추가해 더 정직하게 판단하도록 수정.
- 상세 근거는 `changelog.md`의 2026-07-26 항목 참고.

### 6. 미확인 상태로 남은 것 (다음 확인 필요)
- Alpha Vantage 커넥터를 실제로 연결한 아침 브리핑 클라우드 테스트 실행(`cse_016rFqXmXADwXiN87dSDJMUf`, 2026-07-25 17:41:55 UTC 시작)이 **완료 여부 미확인** — 이 글 작성 시점까지 GitHub에 해당 커밋이 안 올라옴. claude.ai 루틴 페이지(`claude.ai/code/routines/trig_01QXb8haMwgLrHGCZLJoK2Rq`)에서 실행 기록 확인 필요. 실패했다면 원인(권한 승인 문제 등) 파악해서 고쳐야 함.
- Make.com 이메일 발송이 실행 기록상 성공인데 왜 안 보이는지 원인 미해결.

### 7. 주말 스케줄 중복 문제 (2026-07-26 07:04 KST 아침 브리핑 실행 중 발견)
- `portfolio_log.py`의 `open`에는 market+call_date 중복 방지 로직이 없다. 주말(토/일)에는 국내·미국 "다음 거래일"이 며칠 연속 같은 월요일을 가리키는데, 매일 아침 브리핑이 실행되면서 같은 거래일에 콜을 여러 번 열려는 상황이 발생했다 (`predictions/2026-07-25.md`, `predictions/2026-07-27.md`, 그리고 이번 2026-07-26 실행 모두 실질적으로 월요일 7/27 장을 대상으로 함).
- 이번 실행에서는 국내/미국 신규 오픈을 생략(기존 open 유지)하고, 코인도 같은 날 새벽에 이미 오픈된 포지션이라 청산 시점이 아니어서 그대로 뒀다. 상세 사유는 `predictions/2026-07-26.md` 참고.
- **미해결**: (1) 국내/미국 콜을 "거래일 기준 1건"으로 볼지 "달력 날짜 기준 1건"으로 볼지 정책 명확화 필요. (2) 2026-07-25~26 새벽에 겹쳐 실행된 두 세션의 코인 콜(하락 vs 약보합)이 서로 다른데 아직 어느 쪽이 규칙에 더 부합하는지 검토 안 됨. (3) 재발 방지하려면 `portfolio_log.py open`에 동일 market+call_date의 open 행이 있으면 에러를 내는 가드를 추가하는 게 근본적 해결책일 수 있음 — 사용자 확인 후 적용 검토.

### 8. 텔레그램 발송 전면 차단 + 실행 시각/컨텍스트 날짜 불일치 (2026-08-03 07시대 아침 브리핑 실행 중 발견)
- **텔레그램 API 차단**: 기존 코드(`urllib.request`로 `api.telegram.org` 직접 호출) 3통 모두 `Tunnel connection failed: 403 Forbidden`으로 실패(2초 후 1회 재시도도 동일하게 실패). `/root/.ccr/README.md` 확인 결과 "403/407은 조직 egress 정책이 해당 호스트를 막은 것 — 재시도하거나 우회하지 말고 보고할 것"이라고 명시돼 있어, 5번 항목의 CoinGecko/Yahoo Finance 차단과 같은 종류의 조직 정책 차단으로 판단. `ListConnectors`로 확인한 결과 텔레그램용 MCP 커넥터는 없음(Gmail·Google Calendar·주식api만 연결됨, 그마저 Gmail/Calendar는 이 세션에 비활성) — Alpha Vantage 때처럼 "Anthropic 인프라를 우회 경로로 쓰는" 대안이 현재는 없다. **미해결**: 텔레그램 발송을 계속 이 방식(raw urllib)으로 시도하는 한 클라우드 예약 실행에서는 구조적으로 항상 실패할 가능성이 높음 — 사용자가 세션 네트워크 정책에서 `api.telegram.org`를 허용 목록에 추가할 수 있는지 확인 필요. 오늘(8/3) 브리핑은 이 문제로 텔레그램 발송에 실패했고, 대신 `predictions/`·`reports/`·git 커밋으로 기록만 남겼다.
- **실행 시각과 세션 주입 날짜 불일치**: 세션 컨텍스트에 주입된 "오늘 날짜"는 2026-08-02였으나, 실제 시스템 클록(`date`, `TZ=Asia/Seoul date`)은 2026-08-03 07:05 KST를 가리켰다. CLAUDE.md/작업 지침이 "파일명 날짜는 실행 시점의 실제 오늘 날짜(UTC+9)"를 명시하고 있어 시스템 클록을 우선했다. 부수적으로, 전날(8/2) 브리핑 산출물이 로컬 커밋(895a2da)까지만 되고 `origin/main`에 푸시되지 않은 채 남아 있던 것도 함께 발견해 이번 실행 시작 시 먼저 푸시했다. **미해결**: 왜 세션 컨텍스트 날짜가 하루 뒤처졌는지(예약 트리거 자체가 지연 실행됐는지, 컨텍스트 캐싱 문제인지)와 왜 8/2 커밋이 푸시 단계에서 누락됐는지 원인 미파악 — 다음 실행에서 같은 증상이 재발하는지 지켜볼 것.

### 9. 텔레그램 차단 3회 연속 재확인 + 세션 날짜 지연 2회째 재발 (2026-08-04 07시대 아침 브리핑 실행 중 발견)
- **텔레그램 API 차단 재확인**: 8/4 아침 브리핑에서도 국내/미국/코인 3통 모두 재시도(2초 후 1회)까지 포함해 전량 `Tunnel connection failed: 403 Forbidden`으로 실패. `curl $HTTPS_PROXY/__agentproxy/status`로 확인한 `recentRelayFailures`에 `api.telegram.org:443` CONNECT가 조직 정책으로 거부(`connect_rejected`, "gateway answered 403 to CONNECT")된 기록이 다수 남아 있어 8/3에 이어 구조적 차단이 그대로 유지되고 있음을 재확인했다. `/root/.ccr/README.md`가 "조직 정책 거부(403/407)는 재시도하거나 우회하지 말고 보고할 것"이라고 명시하므로, 이 문제는 코드로 해결할 수 없고 **세션 네트워크 정책에서 `api.telegram.org`를 허용 목록에 추가하는 사용자 조치가 필요**하다 — 이번 실행 결과도 predictions/·reports/·git 커밋으로만 기록을 남겼다. 참고로 같은 상태 점검에서 `api.coingecko.com` 역시 동일한 조직 정책 차단(403)이 재확인됨(5번 항목과 일관).
- **세션 날짜 지연 재발(2회째)**: 세션 컨텍스트에 주입된 "오늘 날짜"가 이번에도 2026-08-03으로, 실제 시스템 클록(2026-08-03 22:05 UTC = 2026-08-04 07:05 KST) 대비 하루 늦게 표기됐다. 8/3 실행 때(8번 항목)와 동일한 패턴이 재발한 것으로, 일회성이 아니라 이 예약(아침 브리핑)의 반복적인 특성일 가능성이 있다 — 원인은 여전히 미파악이나, 매번 시스템 클록을 우선하는 CLAUDE.md 원칙으로 안전하게 우회되고는 있음. 다음 실행에서도 재발하는지 계속 지켜볼 것.

### 10. 텔레그램 차단 4회 연속 재확인 (2026-08-04 17시대 마감 비교 브리핑 실행 중 발견)
- **텔레그램 API 차단 재확인(3일 연속, 마감 브리핑에서는 처음 확인)**: 8/4 마감 비교 브리핑에서도 재시도(2초 후 1회) 포함 전량 `Tunnel connection failed: 403 Forbidden`으로 실패. `curl $HTTPS_PROXY/__agentproxy/status`의 `recentRelayFailures`에도 `api.telegram.org:443` CONNECT가 `connect_rejected`(조직 정책 거부)로 재확인됨 — 8번(8/3 아침)·9번(8/4 아침) 항목과 동일한 증상이 하루 안에서도(아침→저녁) 재발해, 시간대와 무관하게 상시 차단 상태임을 확인했다. 8번 항목에서 이미 지적한 대로 이 문제는 코드로 해결 불가 — **세션 네트워크 정책에서 `api.telegram.org` 허용 필요(사용자 조치 대기 중, 아직 미해결)**. 이번 실행도 predictions/·results/·git 커밋으로만 기록을 남겼고, 실패 사실을 최종 요약과 사용자 알림(PushNotification)에 명시했다.

### 11. 텔레그램 차단 5회 연속 재확인 (2026-08-05 07시대 아침 브리핑 실행 중 발견)
- **텔레그램 API 차단 재확인(5회 연속, 4일째)**: 8/5 아침 브리핑에서도 국내/미국/코인 3통 모두 재시도(2초 후 1회) 포함 전량 `Tunnel connection failed: 403 Forbidden`으로 실패. `curl $HTTPS_PROXY/__agentproxy/status`의 `recentRelayFailures`에도 `api.telegram.org:443` CONNECT가 `connect_rejected`(조직 정책 거부)로 재확인됨 — 8번(8/3 아침)·9번(8/4 아침)·10번(8/4 저녁) 항목에 이어 4일째, 5회째 동일 증상. `api.coingecko.com`도 동일하게 차단 재확인(기존과 일관). 이 문제는 코드로 해결 불가 — **세션 네트워크 정책에서 `api.telegram.org` 허용이 반드시 필요**(사용자 조치 대기 중, 아직 미해결). 이번 실행도 predictions/·reports/·git 커밋으로만 기록을 남겼고, 실패 사실을 최종 요약과 PushNotification에 명시했다.
- **참고**: HEAD가 detached 상태로 시작됐으나(`main`이 아닌 커밋에 체크아웃돼 있었음), `git fetch` 확인 결과 origin/main과 로컬 상태가 실제로는 일치했음(로컬 캐시된 origin/main 참조가 stale했을 뿐, 실제 누락된 커밋은 없었음) — `git checkout -B main HEAD`로 정상화 후 이번 브리핑 커밋을 추가해 푸시함. 다음 실행에서 HEAD가 detached 상태로 시작하는지 계속 지켜볼 것(원인 미파악이나 지금까지는 실질적 데이터 손실은 없었음).

## 알려진 한계 / 다음에 볼 것
- `rules.md` v1은 수급 주체별(외국인/기관) 매매동향, 거시 지표(금리/환율)를 아직 반영하지 않음.
- 백테스트의 "전체 적중률" 지표는 위에서 설명한 이유로 그 자체로는 신뢰하기 어려움 — 방향성 콜만 집계한 적중률, Sharpe 유사 지표, 누적수익 위주로 판단할 것.
- 실전(포워드 테스트) 기록은 아직 거의 없음 — 클라우드 예약이 정상 작동하기 시작하면 `portfolio/ledger.csv`에 매일 쌓이기 시작함. 최소 20건(시장별) 쌓이기 전까지는 승률/MDD를 참고용으로만 볼 것 (`portfolio/policy.md` 참고).
- 폴더 구조: 이 프로젝트 관련 내용은 `01_주식프리핑_프로젝트`에, 무관한 "AI 부업 전자책" 콘텐츠는 `02_부업_수익_만들기_01_전자책`으로 분리 이동함 (원래 `claude_project/부업_수익만들기`에 섞여 있었음).
- 미국 시장에만 적용 중인 RSI 신호처럼, "신호가 시장마다 달라야 한다"는 게 확인됐는데 `rules.md` 본문은 아직 모든 시장에 공통된 신호 목록 구조라 이 발견을 정식 반영하려면 문서 구조 자체를 바꿔야 함 — 사용자 확인 후 진행.
