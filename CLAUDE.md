# CLAUDE.md

이 프로젝트(주식/코인 브리핑 및 예측 시스템)를 함께 만들어갈 때 지키는 행동 원칙.
Andrej Karpathy가 정리한 LLM 코딩 실수 방지 가이드(multica-ai/andrej-karpathy-skills)를 기반으로 하며, 이 프로젝트에 맞게 보강했다.

**트레이드오프:** 아래 원칙은 속도보다 신중함을 우선한다. 사소한 작업에는 판단력을 발휘해 유연하게 적용한다.

## 1. 코딩 전에 생각하기

**추측하지 않는다. 헷갈리는 걸 숨기지 않는다. 트레이드오프를 드러낸다.**

- 구현 전에 가정을 명시한다. 확실하지 않으면 묻는다.
- 해석이 여러 개면 하나를 조용히 고르지 않고 모두 제시한다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반박한다.
- 불명확하면 멈추고, 무엇이 헷갈리는지 말하고, 질문한다.

## 2. 단순함 우선

**문제를 푸는 데 필요한 최소한의 코드. 추측성 기능은 넣지 않는다.**

- 요청받지 않은 기능은 추가하지 않는다.
- 한 번만 쓰는 코드에 추상화를 만들지 않는다.
- 요청받지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 에러 처리를 만들지 않는다.

## 3. 외과적 수정

**건드려야 할 곳만 건드린다. 내가 만든 것만 정리한다.**

- 기존 코드/주석/포맷을 임의로 "개선"하지 않는다.
- 깨지지 않은 걸 리팩터링하지 않는다.
- 기존 스타일을 따른다, 내 취향과 달라도.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 지우지 않는다.

## 4. 목표 기반 실행

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

여러 단계 작업은 다음처럼 계획을 먼저 말한다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
```

---

## 이 프로젝트 고유 원칙 (주식/코인 예측 시스템)

- **투자 자문이 아니다.** 모든 예측/의견은 규칙 기반 시스템을 검증·개선하기 위한 실험이며, 실제 투자 판단의 근거로 제시하지 않는다. 브리핑에는 매번 이 점을 짧게 명시한다.
- **모든 예측은 기록한다.** 예측 시점, 근거(전날 뉴스/변동성), 예측 방향(상승/하락/보합)을 저장해야 다음날 결과와 비교할 수 있다. 기록 없는 예측은 발전 루프에 쓸모가 없다.
- **비교는 정직하게.** 예측이 틀렸을 때 이유를 얼버무리지 않는다 — 어떤 가정이 틀렸는지, 어떤 신호를 놓쳤는지 구체적으로 짚는다.
- **규칙은 점진적으로 바꾼다.** 하루치 결과로 규칙 전체를 갈아엎지 않는다. 작은 조정 → 다음날 검증 → 누적된 패턴이 보일 때만 구조적 변경. rules.md는 직접 덮어쓰지 않고 changelog.md에 근거와 함께 추가한 뒤, 패턴이 쌓였을 때만 rules.md 본문에 반영한다.
- **출처를 남긴다.** 뉴스/시세를 요약할 때는 원문 링크를 함께 제공해 사용자가 직접 확인할 수 있게 한다.

**이 원칙이 잘 지켜지고 있다는 신호:** 예측 기록이 매일 누적되고, 오답의 원인이 구체적으로 설명되며, 규칙 변경이 작고 검증 가능한 단위로 이루어질 때.
